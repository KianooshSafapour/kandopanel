jQuery(document).ready(function ($) {
    jQuery('.hasDatepicker').persianDatepicker({
        initialValue: false,
        format: 'YYYY-MM-DD',
        initialValueType: 'persian',
    });
});